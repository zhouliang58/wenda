<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zl.dao.CommentDAO">

    <insert id="addComment" parameterType="com.zl.model.Comment" >
        <selectKey resultType="int" order="BEFORE" keyProperty="id">
            select SEQ__TB_COMMENT1_ID.nextval as id from dual
        </selectKey>
        insert into tb_comment(id , user_id, content, created_date, entity_id, entity_type, status)
        values(#{id},#{userId},#{content},#{createdDate},#{entityId},#{entityType},#{status})
    </insert>

</mapper>
