<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zl.dao.FeedDAO">
    <sql id="table">tb_feed</sql>
    <sql id="selectFields">id, created_date,user_id, data, type
    </sql>
    <select id="selectUserFeeds" resultType="com.zl.model.Feed">
        SELECT id, created_date,user_id, data, type FROM (
        SELECT id, created_date,user_id, data, type, ROWNUM RN FROM tb_feed
        WHERE id BETWEEN 0 and #{maxId}
        <if test="userIds.size() != 0">AND user_id in
            <foreach item
                             ="item" index="index" collection="userIds"
                         open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
            ORDER BY id DESC
        )
        WHERE RN BETWEEN 0 and #{count}
    </select>

    <insert id="addFeed" parameterType="com.zl.model.Feed" >
        <selectKey resultType="int" order="BEFORE" keyProperty="id">
            select SEQ__TB_FEED_ID.nextval as id from dual
        </selectKey>
        insert into tb_feed(id, created_date,user_id, data, type) values(#{id},#{createdDate},#{userId},#{data},#{type})
    </insert>

</mapper>
